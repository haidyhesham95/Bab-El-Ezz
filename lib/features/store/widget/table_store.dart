import 'package:flutter/material.dart';

class TableStore extends StatelessWidget {
  const TableStore({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
